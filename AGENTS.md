## Agent skills

### Issue tracker

GitHub Issues in `tsironis13/full-stack-nx-workspace`; use the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles mapped to this repo’s labels (e.g. `ready-for-agent` → `Sandcastle`). See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context Nx layout: root `CONTEXT-MAP.md`, per-area `docs/<area>/CONTEXT.md`, workspace ADRs in `docs/adr/`, optional `docs/<area>/adr/`. See `docs/agents/domain.md`.
